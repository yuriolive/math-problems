[project]
name = "math-problems"
version = "0.1.0"
description = "Neuro-Symbolic Search Pipelines for Erdős Problems #1 and #64"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openevolve>=0.3.2",
    "z3-solver>=5.1.0",
    "pyyaml>=6.0",
    "tqdm>=4.64.0",
]

[tool.uv]
package = false
